package com.example.pasteleria1000sabores.ui.theme

import androidx.compose.ui.graphics.Color

// ===== COLORES DE PASTELERÍA 1000 SABORES =====

// Color de Fondo Principal
val CremaPastel = Color(0xFFFFF5E1)            // Fondo principal - ambiente cálido

// Colores de Acento
val RosaSuave = Color(0xFFFFC0CB)              // Rosa suave - botones y elementos dulces
val Chocolate = Color(0xFF8B4513)              // Chocolate - tradición y calidez

// Colores de Texto
val MarronOscuro = Color(0xFF5D4037)           // Texto principal - buen contraste
val GrisClaro = Color(0xFFB0BEC5)              // Texto secundario - descripciones

// Colores complementarios
val Blanco = Color(0xFFFFFFFF)
val Negro = Color(0xFF000000)

// ===== COLORES ADICIONALES PARA COMPONENTES SHADCN =====

// Colores primarios
val Primary = RosaSuave                        // Alias para rosa suave
val PrimaryLight = Color(0xFFFFD6E0)           // Rosa más claro

// Colores de superficie
val Surface = Color(0xFFFFFFFF)                // Fondo de tarjetas (blanco)
val Muted = Color(0xFFF5F5F5)                  // Fondo deshabilitado/muted
val MutedForeground = Color(0xFF9E9E9E)        // Texto deshabilitado
val Border = Color(0xFFE0E0E0)                 // Bordes

// Colores de texto
val Foreground = MarronOscuro                  // Texto principal
val ForegroundMuted = Color(0xFF757575)        // Texto secundario/iconos
val ForegroundSubtle = Color(0xFFBDBDBD)       // Placeholders y texto sutil

// Colores de focus/ring
val Ring = RosaSuave                           // Color de focus en inputs

// Colores secundarios
val Beige = Color(0xFFF5E6D3)                  // Beige claro
val LightBrown = Color(0xFFE8D5C4)             // Marrón claro

// Colores neutros
val PaleGray = Color(0xFFF8F9FA)               // Gris muy pálido

// Colores de estado - Success (Verde)
val Success = Color(0xFF10B981)                // Verde éxito
val SuccessLight = Color(0xFFD1FAE5)           // Verde claro

// Colores de estado - Destructive (Rojo)
val Destructive = Color(0xFFDC2626)            // Rojo error/eliminar
val DestructiveLight = Color(0xFFFEE2E2)       // Rojo claro
val DestructiveHover = Color(0xFFB91C1C)       // Rojo más oscuro

// Colores de estado - Warning (Amarillo/Naranja)
val Warning = Color(0xFFF59E0B)                // Naranja advertencia
val WarningLight = Color(0xFFFEF3C7)           // Amarillo claro